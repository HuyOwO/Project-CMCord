# Chỉ cần set khi frontend và backend KHÔNG chạy chung 1 máy
# (vd: máy A chạy backend, máy B chỉ chạy frontend để test real-time chung).
# Để trống nếu backend chạy cùng máy với frontend (dev bình thường).
#
# VITE_API_URL=http://<IP-may-chay-backend>:5000
VITE_API_URL=
